void bfs(VERTEX* vertices) {
            char queue[1000] = "";
 
            VERTEX* v = vertices;
 
            printf("%c ", v->c);
            v->isVisited = 1;
            push(stack, v->c);
 
            while (strlen(queue) > 0) {
 
            }
}